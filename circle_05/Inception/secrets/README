To set the db_password.txt and db_root_password.txt you can use the command below inside the secrets folder:

echo "example-password" > db_password.txt
echo "example-password" > db_root_password.txt

To set the nginx ssl certificate you can use this command below:

mkdir certs && openssl req -x509 -newkey rsa:4096 -keyout ./certs/nginx.key -out ./certs/nginx.crt -sha256 -days 3650 -nodes
